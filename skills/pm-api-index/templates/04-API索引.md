# API 索引

> 由 pm-api-index 自动生成。每条 API 含【位置: 文件:行】【归属: 对外/内部】；对外 API 与核心 API 含调用关系。
> 对外 = 跨进程/跨模块契约入口（HTTP/RPC/SPI/CLI/事件/导出函数）；内部 = 单模块内分层调用（Service/DAO/工具）。

## 形态覆盖声明

| API 形态 | 发现情况 | 收录数 |
|----------|---------|--------|
| HTTP 路由 | {发现 N 个} | {n} |
| RPC 服务接口 | {发现 N 个 / 未发现} | {n 或 0} |
| SPI / 跨模块服务接口 | {发现 N 个 / 未发现} | {n 或 0} |
| 前端调用层 | {发现 N 个 / 未发现} | {n 或 0} |
| CLI 命令 | {发现 N 个 / 未发现} | {n 或 0} |
| 事件 handler / 消息消费者 | {发现 N 个 / 未发现} | {n 或 0} |
| 导出函数（library） | {发现 N 个 / 未发现 / 不适用} | {n 或 0} |

**覆盖率**：共收录 {已收录} / {总数} 个 API，覆盖率 {percent}%。{若采样：采样策略说明；若全量：注明"全覆盖"}。

## 全局约定

{若项目有统一请求/响应报文、错误码、鉴权约定、版本约定等跨 API 的公共契约，在此简述并指向定义文件；若无则注明"无统一约定"。}

## {模块/领域名称 1}

> {该模块/领域的一句话说明，含部署位置/端口/职责}

### 对外 API

{HTTP 路由用表格；其他形态用列表。每条含标识、用途、位置:文件:行、调用方、下游。}

| 方法 | 路径 | 用途 | 位置 | 调用方 | 下游 |
|------|------|------|------|--------|------|
| {HTTP 方法} | {路径} | {purpose} | `{文件路径}:{行号}` | {caller} | {downstream services/modules} |

{RPC/SPI/CLI/事件/导出函数：用列表呈现，每条标注形态类型}

- **{形态}：{标识/签名}** — {用途}
  - 位置：`{文件路径}:{行号}`
  - 调用方：{caller}
  - 下游：{downstream}

### 内部 API

{Service 方法 / DAO 方法 / 核心 helper。每条含标识、用途、位置:文件:行。核心内部 API 标注"被调用方"。}

| 方法/签名 | 用途 | 位置 | 被调用方 |
|----------|------|------|---------|
| {signature} | {purpose} | `{文件路径}:{行号}` | {callers；非核心可留"—"} |

## {模块/领域名称 2}

{同上结构}

## 调用关系总览

{用文本 DAG 或列表展示核心 API 的调用链：典型形式为 "前端调用层 → HTTP 路由 → Service → DAO → 数据库/外部资源"；RPC 跨服务调用；SPI 解耦调用。至少覆盖对外 API 的主干调用链。}

## 未收录说明

{诚实声明未收录的内容及原因：省略的框架继承方法（约 N 个）、Mapper XML、私有 helper、etc.}
