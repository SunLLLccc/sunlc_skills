# 技术栈与架构

> 由 pm-techstack-generic 自动生成

## 一、开源框架清单

> 按项目实际的技术维度分组（如语言/运行时/模块/层级），根据项目类型灵活调整。
> 示例分组：后端 / 前端 / 共享工具 / 构建工具；纯后端项目省略前端章节，纯 library 项目用单一分组等。

### {分组名称，如"后端" / "前端" / "构建工具" 等}

| 框架/库 | 版本 | 用途 | 配置文件位置 |
|---------|------|------|-------------|
| {name} | {version} | {purpose} | {config file path} |

## 二、项目自封装框架

> 以下为项目自建的内部框架/SDK/基类/中间件，与开源第三方库明确区分。

### {自封装框架名称}

- **所在模块/目录**：{module path}
- **设计意图**：{一句话说明为什么存在这个自封装}
- **核心组件**：

| 组件 | 文件路径 | 职责 |
|------|---------|------|
| {class/function name} | {file path} | {responsibility} |

- **被依赖方**：{列出依赖此自封装的模块/文件}

## 三、架构模式

### 整体架构

{用文本图或分层描述展示整体架构}

### 分层结构

{描述项目的分层架构，如 Controller-Service-Repository 等}

### 数据流

{描述核心请求的数据流路径}

### 设计模式

{列出项目显著使用的设计模式}

## 四、模块依赖关系

### 模块依赖图

{用文本 DAG 或矩阵展示模块间的依赖关系}

### 解耦接口

| 接口 | 定义位置 | 实现位置 | 消费者 |
|------|---------|---------|--------|
| {interface name} | {definition module} | {implementation module} | {consumer module} |

### 外部基础设施依赖

| 基础设施 | 用途 | 对接模块 |
|---------|------|---------|
| {infra} | {purpose} | {module} |
