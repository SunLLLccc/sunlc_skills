# 构建打包部署

> 由 pm-build-deploy 自动生成。每条命令/部署方式/版本号附【来源】【标注】字段。
> 标注：已验证=配置文件中直接找到；推断=未直接找到/来自文档/需人工确认；未发现=探测后确认不存在。
> 本文档只梳理现状，不含顾问建议（推荐改造、建议流水线等不在本 skill 职责内）。

## 多构建并存声明

> 若项目仅单一构建，注明"本项目为单一 {构建工具} 构建"。若多构建并存，逐项声明。

| 构建形态 | 模块/目录 | 配置文件 |
|----------|----------|----------|
| {构建工具，如 Maven / npm / Vite} | {module} | {config path} |

## 一、环境要求

### {运行时，如 JDK / Node.js / Python / Go / 数据库}

| 要求 | 版本 | 来源 | 标注 |
|------|------|------|------|
| {运行时名} | {version 或"配置未声明"} | `{config path}: {field}` | 已验证 / 推断（{依据，如"来自 README，配置未声明"}） |

> {若某运行时配置未声明：注明"配置未声明 {运行时} 版本，需人工确认"；README 版本可作推断线索列出}

## 二、依赖安装

### {模块/构建名}

| 命令 | 来源 | 标注 |
|------|------|------|
| `{命令字面量，如 npm install / mvn 无独立步骤}` | `{config path}: {field 或"无独立步骤，构建时自动解析"}` | 已验证 / 推断 |

## 三、开发启动

### {模块/构建名}

| 命令 | 来源 | 标注 | 备注 |
|------|------|------|------|
| `{命令，如 npm run dev / java -jar ...}` | `{config path}: {field}` | 已验证 / 推断 | {如开发代理、端口、需先启动的依赖} |

> {开发代理/端口从配置文件读取的，标已验证附配置路径}

## 四、构建/打包

### {模块/构建名}

| 命令 | 产物 | 来源 | 标注 |
|------|------|------|------|
| `{命令，如 npm run build / mvn clean package}` | `{产物路径，如 dist/ / target/*.jar}` | `{config path}: {field / plugin}` | 已验证 / 推断 |

> {多模块项目区分可执行产物（fat-jar/二进制）与依赖产物（普通 jar/库包），从构建插件配置区分}

## 五、部署方式

### 部署形态探测声明

| 部署形态 | 探测结果 | 配置文件 |
|----------|----------|----------|
| Dockerfile | {发现，附路径 / 未发现} | {path 或 —} |
| docker-compose | {发现 / 未发现} | {path 或 —} |
| Kubernetes | {发现 / 未发现} | {path 或 —} |
| Makefile 部署 target | {发现 / 未发现} | {path 或 —} |
| 静态托管（前端） | {适用，产物 dist/ / 不适用} | — |
| 反向代理配置 | {发现 / 未发现 / 推断需要} | {path 或依据} |

### 实际部署形态

{根据探测结果描述：发现的容器化配置详述（构建阶段、服务编排、部署 target）；未发现时注明"未发现容器化配置，部署形态为 {裸机直跑 / 静态托管}"，并标注这是「推断」（依据：无可执行配置 + 无容器配置）}

| 部署步骤/方式 | 来源 | 标注 |
|---------------|------|------|
| {部署命令/方式} | {config path 或"无配置，推断依据：..."} | 已验证 / 推断 |

> {任何配置未声明的部署细节（JVM 参数、守护进程、反向代理拓扑）必须标「推断」+ 说明依据，不与已验证事实混述}

## 六、环境变量/配置项

### {模块/服务名}

| 配置项（yml 路径 / 配置键） | 环境变量 | 默认值 | 来源文件 | 标注 |
|------------------------------|----------|--------|----------|------|
| {config key} | {env var 或"未外置"} | {default 或"—"} | `{config path}` | 已验证 |

> {敏感配置（密钥/密码）带默认值的，注明"生产必须替换"（配置事实）}
> {配置文件不存在的模块，注明"未发现配置外置机制"}

## 七、CI/CD

### CI 配置探测声明

| CI 系统 | 探测结果 | 配置文件 |
|---------|----------|----------|
| GitHub Actions | {发现 / 未发现} | {path 或 —} |
| GitLab CI | {发现 / 未发现} | {path 或 —} |
| Jenkins | {发现 / 未发现} | {path 或 —} |
| 其他 | {发现 / 未发现} | {path 或 —} |

### 现有流水线（发现时）

{概述触发条件 / 构建步骤 / 部署步骤 / 密钥管理，引用配置文件路径，标已验证}

> {未发现 CI 配置时，整章替换为"**未发现 CI/CD 配置**：探测了 .github/workflows、.gitlab-ci.yml、Jenkinsfile 等均无自动化流水线。如项目有人工协作规范（CONTRIBUTING.md 中的 PR/分支/版本规则），可作推断线索列出，但禁止补写建议流水线"}

## 附：关键配置文件索引

| 用途 | 路径 |
|------|------|
| {用途，如 父 POM / 前端 package.json / Dockerfile / CI 配置} | `{绝对或项目相对路径}` |
