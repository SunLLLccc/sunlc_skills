# KB 文档抽样校验报告

> 由 pm-verify 自动生成。本报告**只校验不修改**——发现的所有差异写入"不一致项清单"与"待人工确认可疑项清单"，由人工决定是否修订 KB 文档。
> 每条核对含【文档断言】【代码证据（带 `{文件路径}:{行号}`）】【比对结果（四分类）】。
> 核对结果四分类：一致 / 不一致 / 无法核对（推断项）/ 无法核对（断言模糊）。

## 一、总体结论

| 维度 | 评价 |
|------|------|
| 总体准确性 | {基于抽样核对的总体判断，如"较高"/"存在偏差"/"需复核"} |
| 抽样核对规模 | 共核对 {N} 份文档、{M} 条断言（一致 {a} / 不一致 {b} / 无法核对（推断）{c} / 无法核对（模糊）{d}） |
| 严重问题数 | {数}（{说明，如"导致架构误判的硬伤"}） |
| 中等问题数 | {数}（{说明}） |
| 轻微问题数 | {数}（{说明}） |

**校验通过的核心声明**（均与源码一致，举代表性几条）：
- {断言 1}：{代码证据 `{文件路径}:{行号}`}
- {断言 2}：{代码证据}
- {断言 3}：{代码证据}

---

## 二、抽样核对明细（逐条三元组）

> 每条核对含：文档断言（出处）+ 代码证据（`{文件路径}:{行号}`）+ 比对结果（四分类）。

### {NN-文档名}.md

| # | 文档断言（出处） | 代码证据 | 比对结果 |
|---|------------------|----------|----------|
| 1 | {断言内容}（`{NN-文档名}.md: {章节/行}`） | `{文件路径}:{行号}`：{实际字面量} | 一致 / 不一致 / 无法核对（推断项）/ 无法核对（断言模糊） |
| 2 | {断言内容} | `{文件路径}:{行号}`：{实际字面量} | {分类} |
| 3 | {断言内容} | `{文件路径}:{行号}`：{实际字面量} | {分类} |
| ... | ... | ... | ... |

{每份已生成的 KB 文档都至少 3 条核对，覆盖该文档内不同类型断言}

---

## 三、不一致项清单（已验证与代码冲突）

> 以下为本次核对中判定为「不一致」的断言。**本报告只列出，不修订 KB 文档**——修订由人工决定。

### 【{严重度：严重/中等/轻微} {编号}】{一句话标题}

- **涉及文档**：`{NN-文档名}.md: {章节/行}`
- **文档断言**：{文档原话或转述}
- **实际值**：{代码实际字面量}
- **代码证据**：`{文件路径}:{行号}`：{实际内容}
- **影响**：{这条不一致会让读者产生什么误判}

{有多条不一致就列多条；若本次核对无不一致项，整章替换为"**本次抽样核对未发现不一致项**（抽样范围见第六节）。注意：'未发现'基于本次抽样，不等于'全部正确'"}

---

## 四、待人工确认可疑项清单（无法机械核对 / 断言模糊）

> 以下为本次核对中判定为「无法核对」的断言——要么是推断性陈述无机械基准，要么是断言模糊无定位。建议人工复核。

| # | 文档断言（出处） | 类型 | 原因 | 建议 |
|---|------------------|------|------|------|
| 1 | {断言内容}（`{NN-文档名}.md: {章节}`） | 推断项 / 断言模糊 | {为什么无法机械核对} | {如"建议补充具体位置"/"建议人工判断是否成立"} |
| ... | ... | ... | ... | ... |

---

## 五、各文档置信度评估

> 置信度基于**本次抽样核对的实际结果**，非照抄文档自评。

| 文档 | 校验后置信度 | 依据 |
|------|-------------|------|
| 01-001-项目概览.md | {high/medium/low} | {如"抽样 {n} 条全部一致"/"发现 {m} 条不一致"/"推断项占比高"} |
| 02-001-技术栈与架构.md | {等级} | {依据} |
| 03-001-开发规范.md | {等级} | {依据} |
| 04-001-API索引.md | {等级} | {依据} |
| 05-001-构建打包部署.md | {等级} | {依据} |
| README.md | {等级} | {依据} |
| .codebase/scan-result.json | {等级} | {依据} |

{未生成的文档注明"未生成，未纳入校验"}

---

## 六、抽样范围声明（诚实边界）

| 项 | 值 |
|----|----|
| 本次校验的 KB 文档 | {列出已校验的文档清单；未生成的注明"未生成，未纳入"} |
| 抽样核对总条数 | {M} 条（一致 {a} / 不一致 {b} / 无法核对（推断）{c} / 无法核对（模糊）{d}） |
| 抽样方法 | 可验证性优先（版本号/文件:行/配置值/枚举计数/目录结构）+ 每份文档覆盖不同类型断言 |
| 每份文档抽样条数 | {NN-文档名}: {n} 条；{NN-文档名}: {n} 条；... |
| 未抽样的断言如何处理 | **不外推为"全部通过"**。本次"一致"结论仅适用于已抽样的 {M} 条，不代表文档 100% 正确 |

> 即便本次核对全部一致，也仅基于 {M} 条抽样得出，KB 文档的完整正确性需人工复核未抽样部分。

---

## 七、「未发现」声明独立复核

> KB 文档里的"未发现 X"类声明，逐条独立重新探测，确认确不存在（防虚报/防过时）。

| KB 声明"未发现"项 | 复核结果 |
|------------------|---------|
| {未发现 Dockerfile} | ✓ 确认不存在 / ✗ 实际存在（`{路径}`），列为不一致项 |
| {未发现 CI 配置} | ✓ 确认不存在 / ✗ 实际存在 |
| ... | ... |

---

## 八、校验方法说明

本报告通过以下方式交叉核对：
1. **配置文件核对**：{列出本次实际读过的配置文件}
2. **源码行号核对**：用 `grep -n`/Read 逐条比对 KB 声明的 `{文件路径}:{行号}` 与源码实际行号
3. **统计命令核对**：`find`/`ls`/`grep -c` 复核计数类断言
4. **「未发现」项独立复核**：对每条"未发现"声明重新探测
5. **跨文档一致性检查**：同一事实在多份文档重复出现时核对自洽性

**未校验的范围**（诚实声明）：
- {未逐个阅读全部源文件，仅抽样核心类与统计性扫描}
- {未运行构建命令验证构建是否真正通过（构建命令基于配置事实，未实跑）}
- {其他本次未覆盖的范围}
