# KB 文档轻量抽样校验报告（pm-verify-lite）

> 由 pm-verify-lite 自动生成（默认轻量校验）。本报告**只校验不修改**——发现的所有差异写入"不一致项清单"，由人工决定是否修订 KB 文档。
> 每份文档抽样 3-5 条断言，每条含【文档断言】【代码证据（`{文件路径}:{行号}`）】【比对结果（三分类：一致/不一致/无法核对）】。
> 如需全量深度核对（跨文档一致性/未发现独立复核/逐文档置信度），改跑 opt-in 的 `pm-verify`。

## 一、总体结论与风险信号

| 维度 | 评价 |
|------|------|
| 抽样规模 | 共核对 {N} 份文档、{M} 条断言（一致 {a} / 不一致 {b} / 无法核对 {c}） |
| 风险信号 | {低/中/高}——{一句话理由，如"无不一致"/"发现 2 条不一致但非关键文档"/"关键文档 01 出现入口路径不一致，建议升级 full"} |
| 是否建议升级 full | {是/否}——{理由} |

**风险信号判定**：低=无不一致项；中=不一致项 1-2 条且不在关键文档（01/02/04）；高=不一致项 ≥3 条，或任一关键文档（01/02/04）出现不一致——此时建议改跑 `pm-verify`（opt-in）全量核对。

---

## 二、抽样核对明细（逐条三元组）

> 每份已生成的 KB 文档抽样 3-5 条，优先可机械核对的断言（版本号/文件:行/配置值/计数/目录）。

### {NN-文档名}.md

| # | 文档断言（出处） | 代码证据 | 比对结果 |
|---|------------------|----------|----------|
| 1 | {断言内容}（`{NN-文档名}.md: {章节/行}`） | `{文件路径}:{行号}`：{实际字面量} | 一致 / 不一致 / 无法核对 |
| 2 | {断言内容} | `{文件路径}:{行号}`：{实际字面量} | {分类} |
| 3 | {断言内容} | `{文件路径}:{行号}`：{实际字面量} | {分类} |
| 4 | {断言内容} | `{文件路径}:{行号}`：{实际字面量} | {分类} |
| 5 | {断言内容} | `{文件路径}:{行号}`：{实际字面量} | {分类} |

{每份已生成的 KB 文档都给一个上表；推断性内容多的文档如实多标"无法核对"}

---

## 三、不一致项清单（与代码冲突）

> 以下为本次核对判定为「不一致」的断言。**本报告只列出，不修订 KB 文档**——修订由人工决定。

| # | 涉及文档 | 文档断言 | 实际值（代码证据） | 影响 |
|---|----------|----------|--------------------|------|
| 1 | `{NN-文档名}.md: {章节/行}` | {文档原话或转述} | `{文件路径}:{行号}`：{实际字面量} | {这条不一致会让读者产生什么误判} |
| ... | ... | ... | ... | ... |

{若本次无不一致项，整章替换为"**本次抽样核对未发现不一致项**（抽样范围见第四节）。注意：'未发现'基于本次 3-5 条/文档的抽样，不等于'全部正确'——如需深度核对请跑 pm-verify。"}

---

## 四、抽样范围声明（诚实边界）

| 项 | 值 |
|----|----|
| 本次校验的 KB 文档 | {列出已校验文档清单；未生成的注明"未生成，未纳入"} |
| 抽样核对总条数 | {M} 条（一致 {a} / 不一致 {b} / 无法核对 {c}） |
| 每份文档抽样条数 | {NN-文档名}: {n} 条；{NN-文档名}: {n} 条；...（每份 3-5 条） |
| 抽样方法 | 可验证性优先（版本号/文件:行/配置值/计数/目录结构）+ 每份覆盖不同类型 |

> lite 版**不做**：跨文档一致性核对、「未发现」声明独立复核、逐文档置信度评估。本次"一致"结论仅适用于已抽样的 {M} 条，不代表文档 100% 正确。如需深度核对，改跑 opt-in 的 `pm-verify`。
